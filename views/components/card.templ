package components

templ Card(name string, value string ) {
    <div class="rounded-xl bg-gray-50 p-2 shadow-sm">
        <div class="flex p-4">
            <!-- {Icon ? <Icon class="h-5 w-5 text-gray-700" /> : null} -->
            <h3 class="ml-2 text-sm font-medium">{name}</h3>
        </div>
        <p
            class="rounded-xl bg-white px-4 py-8 text-center text-2xl"
        >
            {value}
        </p>
    </div>
}